# Help with the Cold 🥶

Global temperature conversion

## Install and Run

We suggest a virtual environment (see below) - in that venv...

1. `pip install -r requirements.txt`
2. `python main.py`

## Virtual Environment

1. `python -m venv venv`
2. Depends upon OS...

    Mac/Linux/Bash: `source venv/bin/activate`
    Windows (PS): `.\venv\Scripts\Activate.ps1`
    Windows (cmd): `venv\Scripts\activate.bat`

Do cool things

3. Once you are done: `deactivate`
